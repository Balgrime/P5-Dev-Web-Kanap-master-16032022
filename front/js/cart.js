class Cart {
    constructor(key, value) {
        this.key = key;
        this.value = value;
    }
};

//let cart = new Cart('cartActif', cartArray);

function addProductToCart(key, value) {
    key = idProductActif + pickedColor;
    value = Quantity;
    return new Cart(key, value);
};